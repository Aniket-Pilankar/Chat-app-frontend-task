export interface VMProps {
  fetchAgain: boolean;
  setFetchAgain: React.Dispatch<React.SetStateAction<boolean>>;
}

export default function useChatboxVM({ fetchAgain, setFetchAgain }: VMProps) {
  return {};
}
